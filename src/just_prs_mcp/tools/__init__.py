"""Tool registration modules, grouped by tier.

- ``catalog``   — essentials: PGS Catalog lookup (search, info, performance, traits).
- ``compute``   — essentials: VCF normalization, PRS computation, and analysis.
- ``extended``  — extra tools (mode == "extended"): batch, downloads, HF upload.
- ``reference`` — extended: reference-panel / pgen scoring (needs ``pgenlib``).
"""
